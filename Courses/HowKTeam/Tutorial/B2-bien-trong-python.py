# Variables
# Tên của biến không được bắt đầu là số
# Tên của biến chỉ chứa các chữ cái, số và '_'
# Tên của biến phân biệt in hoa và thường
"""
    1. Tên của biến không được bắt đầu là số
    2. Tên của biến chỉ cho phép chữ cái, số và '_'
    3. Tên của biến phân biệt in hoa và thường
    4. Có các kiểu dữ liệu
        - Numbers: int, float, complex
        - Strings:
            + (' ')
            + (" ")
            + (\) : quotes
            + (r) : raw strings
            + (""" """)
        - (_) : dùng trong interactive mode, giá trị là biểu thức in cuối cùng
    5. Kiểm tra biến thuộc kiểu dữ liệu: type()
    6. Các toán tử:
        - (/) : returns a float
        - (//) : returns a int
        - (**) : calculate powers
        - (%) : remainder
"""