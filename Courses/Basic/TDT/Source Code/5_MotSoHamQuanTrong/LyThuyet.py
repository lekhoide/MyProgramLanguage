"""
    1. Các hàm toán học: from math
    - sqrt(): căn bặc 2
    - pow(): lũy thừa
    - log(): log
    - log10(): Logarit cơ số 10
    - exp(): tính e
    - degrees(): Đổi radian thành độ
    - radians(): Tính radian 180/PI*x
    - : Tính giá trị tuyệt đối

    2. Các hàm toán học lượng giác:
    - sin(radians())
    - cos(radians(0))
    - tan(radians())
    - cotan = 1 / tanx

    3. Hàm round(số gốc, đơn vị làm tròn): làm tròn số

    4. Hàm time:from  time
    - clock(): tính thời gian chạy lấy end -start
    - sleep(second): thời gian nghỉ

    5. Hàm Random: xuất ngẫu nhiên from random
    - randrange(x,y)

    6. Hàm exit: để thoát phần mềm

    7. Hàm evel: tự tính toán chuỗi phép toán
"""